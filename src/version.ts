// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.15.0';
export const APP_VERSION_NOTE = 'Tạo Card — Auto Creator v4 (goal 104, khép lại đại tu 100-104): pipeline chạy theo SƠ ĐỒ PHỤ THUỘC nên các bước độc lập chạy SONG SONG qua pool nhiều key (đo thật: basic_info xong là lorebook ∥ system_prompt ∥ mes_example cùng lúc, rồi game_ui ∥ regex ∥ first_message) thay vì xếp hàng từng bước; bước regex nay áp luật sắt của goal 103 — regex nào không compile hoặc replaceString vỡ thì BỊ LOẠI đích danh chứ không nhét vào card; Kiểm tra tổng thể lên bản 2: chạy HARNESS ĐỦ VÒNG thật (nạp initvar của thẻ, giả lập form ghi từng biến schema rồi đọc lại) nên bắt được lớp lỗi initvar-một-đằng-schema-một-nẻo mà mọi phép kiểm tĩnh đều lọt, cộng kiểm parse EJS. Trả nốt nợ #43: nút Dừng nay HỦY LUÔN request đang bay (cả Phase 0 phân tích ý tưởng, cả batch lorebook, cả pipeline Minh Nguyệt), ý tưởng + cấu hình được lưu nên đổi tab quay lại không mất, bỏ ép JSON output, và web search tự nới query khi truy vấn hẹp quá không ra kết quả. Kèm 3 fix báo-đỏ-oan tìm ra khi chạy thật: data-var viết dạng /A/B hay A.B không còn bị coi là lệch schema, fence chưa đóng nằm giữa nội dung nay bắt được, và hàm đưa ra global bằng Object.assign(window,…) không còn bị báo nút bấm không chạy.';
