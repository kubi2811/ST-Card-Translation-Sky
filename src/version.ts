// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.1.8';
export const APP_VERSION_NOTE = 'Fix bugNeedFix/39 — TREO MÀN ~20s khi dịch qua luồng So Sánh/Update card. Dựng lại đúng ca của bạn (3 card thật: v1.2 + Quỷ Sự Lục v1.21 + 1.6 → Gộp thông minh → 418/605 field đã có sẵn bản dịch, 187 field cần dịch) rồi ĐO từng khâu thay vì đoán. Bốn điểm nghẽn thật, tất cả đều là main thread (khớp hiện tượng RAM dư – CPU thấp): (1) RAG cross-field: tfidfCosineSimilarity dựng LẠI 2 bảng term + tính magnitude 3 lần cho MỖI CẶP field — với 418 field done và field script 171K ký tự là hàng chục triệu phép/1 field ⇒ ĐO 1.431ms/field, ~34s. Nay cache vector (đã nhân IDF, cap 400 term đại diện) + magnitude tính 1 lần ⇒ 21ms (nhanh 67×). (2) Chuỗi auto-fix EJS chạy SAU mỗi field: chi phí theo SỐ MỤC TỪ ĐIỂN (dựng vài RegExp cho từng mục) nên field chỉ 1.117 ký tự vẫn tốn 145ms ⇒ ~88s cộng dồn. Nay LỌC từ điển theo text trước khi dựng RegExp (mục không xuất hiện thì không pattern nào khớp được — bảo toàn hành vi 100%) ⇒ 5ms (nhanh 27×); field lớn 1.250ms → 249ms. (3) chunkText: findSafeBoundary dò MỌI dấu cách/xuống dòng trong cửa sổ 10K, mỗi lần isSafeBoundary quét lại tiền tố ⇒ 499-1.123ms/field code. Nay memo theo vị trí + trần 12 lần dò/dấu + bước fallback 200 ⇒ 75-407ms. (4) BÃO RE-RENDER: useTranslation() và App.tsx subscribe TOÀN store nên 560+ set() trong burst dịch re-render cả cây, vô hiệu mọi fix selector trước đó. Nay useTranslation dùng Proxy đọc getState (0 subscription), App + 11 component (FieldEditor/VerifyPanel/ExportPanel/TranslateConfig/MvuSync/EjsSync/CardPreview/ExternalLink/ProxyConfig/FileUpload/PresetImport…) chuyển selector HẸP + throttle fields. (5) Chống BÃO TOAST: lỗi hàng loạt từng bắn hàng trăm toast (mỗi cái sống 5s) — nay gộp trùng (×N) + trần 5 dòng. + yield main thread mỗi việc trong runWorkerPool/mỗi field. +8 test khoá hành vi & hiệu năng (458 xanh). | 2.1.7: bug 37-38 + khoá từ điển MVU.';
