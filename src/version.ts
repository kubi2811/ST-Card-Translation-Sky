// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.18';
export const APP_VERSION_NOTE = 'Trợ Lý AI — KỶ LUẬT XỬ LÝ DỮ LIỆU LỚN & CHUNKING (bug 24, đi cặp với fix chia PHẦN ở 1.99.17): thêm vào SYSTEM_INSTRUCTION mục kỷ luật mới — (1) gặp nhãn "(PHẦN i/N)" phải xử lý DỨT ĐIỂM TRỌN VẸN từng phần, cấm tóm tắt/cắt bớt để tiết kiệm token; (2) ĐỒNG BỘ 1:1: vào bao nhiêu mục/dòng thì ra đúng bấy nhiêu, cấu trúc phân cấp khớp 100%, không gộp dòng; (3) dữ liệu quá dài → CHỦ ĐỘNG cảnh báo + đề xuất chia nhỏ TRƯỚC khi dịch; file lỗi cú pháp sẵn → báo lỗi nằm đâu, hỏi sửa trước hay dịch nguyên trạng; (4) NHẤT QUÁN xuyên phần: tự lập bảng thuật ngữ (tên riêng/xưng hô/địa danh) từ phần đầu và bám xuyên suốt; (5) dịch dữ liệu cấu trúc: CHỈ dịch values, cấm đụng keys/biến/tags/regex/ngoặc, cấm chèn bình luận vào trong khối JSON; (6) kết quả dịch luôn trong code block chuẩn. Tab Tạo MVU-Zod thêm quy tắc PHẦN i/N tương ứng. | 1.99.17: hết cắt cụt file đính kèm 100k.';
