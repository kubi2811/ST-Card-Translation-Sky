// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.5.0';
export const APP_VERSION_NOTE = '📜 TOOL MỚI "DỊCH SCRIPT" — tab mới trên thanh bên, dịch bundle JS TavernHelper (1-3MB, kiểu script 智脑/trí não) Trung → Việt ngay trong app chính: dùng chung pool provider/API key + engine đa luồng của Dịch Card, không phải cấu hình lại gì. Nguyên tắc vàng: CODE KHÔNG BAO GIỜ gửi cho AI — chỉ gửi các chuỗi tiếng Trung đã tách (surgical extract), nên cấu trúc script được bảo toàn theo thiết kế. Quy trình: làm đẹp code (Prettier, chạy trong Worker — file 1.5MB không đơ UI giây nào) → Bảng tên Pha 0 (1 lượt AI, sửa tay được) → dịch song song theo lô → ghép lại đúng chỗ → vá regex → kiểm sức khoẻ (JS hợp lệ, ngoặc không lệch, chuỗi sót). Điểm đặc biệt: object key tiếng Trung ({子时:"00:00"}), CSS class, truy cập thuộc tính được GIỮ NGUYÊN có chủ đích (đổi là vỡ script); regex khớp nhãn Trung được GIỮ HÁN + THÊM nhánh Việt (/秋青子/ → /(?:秋青子|Thu Thanh Tử)/) để chat cũ vẫn khớp — regex sửa xong phải compile được, không thì tự hoàn nguyên. Tiến độ tự lưu mỗi vài giây vào cache máy → F5/tắt trình duyệt giữa chừng, mở lại nạp đúng file là dịch tiếp miễn phí. Có toggle NSFW, nút Dịch lại mục lỗi, tải file .vi.js. i18n đủ vi/en/zh, 20 test mới. | 2.4.0: khởi động nhẹ — start.bat chỉ mở Dịch Card, tool khác tự bật khi bấm tab.';
