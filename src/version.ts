// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.12';
export const APP_VERSION_NOTE = 'Trợ Lý AI nâng cấp lớn theo 5 yêu cầu: 1) VAI TRÒ = chuyên gia tối ưu/sửa lỗi/dịch chuyên sâu card v2/v3 + Lorebook + Regex + TavernHelper (HTML/JS/CSS/JSON) KIÊM bạn đồng hành trò chuyện/tư vấn. 2) GIAO TIẾP như Gemini/Claude: tự nhiên có cảm xúc, BRAINSTORM (bí ý tưởng → gợi 2-3 hướng + ưu/nhược), giải thích cặn kẽ kèm ví dụ; FIX BUG NHỚ HỘI THOẠI — tab Trò Chuyện trước đây CHỈ gửi câu hiện tại nên AI quên sạch lượt trước, nay gửi kèm 10 lượt gần nhất. 3) DỊCH: bảo toàn tuyệt đối {{char}}/{{user}}/macro/<AI_ACTION>/biến logic/thẻ HTML-JSON; chỉ dịch văn bản hiển thị; tên biến MVU dấu cách theo từ điển (khớp chuẩn v1.99.9); văn phong bám thể loại. 4) SỬA FILE: quét lỗi cú pháp phổ biến, sửa triệt để GIỮ logic gốc, regex an toàn iOS. 5) OUTPUT: giải thích thân thiện lỗi ở đâu/sửa gì TRƯỚC, code TRỌN VẸN trong code block hoặc <AI_ACTION> (có xác nhận). Lời chào modal mới 3 locale. | 1.99.11: shim preview đối chiếu JS-Slash-Runner.';
