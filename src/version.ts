// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.16.13';
export const APP_VERSION_NOTE = 'Tạo Card — fix bug 125 (Sinh EJS điều khiển trong Lorebook làm thẻ nổ lỗi đỏ khi chơi). Hai lỗi user chụp có hai gốc riêng. Một: tool dạy AI và tự sinh hàm setEntryEnabled(comment, bool) — đối chiếu thẳng source ST-Prompt-template thì hàm này KHÔNG TỒN TẠI, nên controller chết ngay dòng gọi đầu ("setEntryEnabled is not defined"). Extension không cho TẮT entry từ EJS; nó chỉ cho KÍCH HOẠT entry đang tắt bằng await activewi(\'tên entry\', true) — và thiếu tham số true thì lời gọi chạy êm nhưng chẳng đẩy được entry nào vào prompt. Nay toàn tuyến đổi sang mô hình đúng: entry cần điều khiển được TẮT SẴN trong cấu hình rồi bật có điều kiện; entry chết/trùng thì tắt bằng cấu hình chứ không sinh entry PriorityGate vô dụng nữa. Hai: lỗi "Unexpected token \'<\' while compiling ejs" là do template tự sinh mở khối <%_ rồi mở lại lần nữa mà không đóng — hàm lint của extension khi còn trong khối hở sẽ trả nguyên mọi thẻ phía sau, nên ký tự \'<\' lọt vào JS. Nay dựng lại đúng máy trạng thái lint đó thành bộ kiểm chạy TRƯỚC khi ghi vào thẻ: code nào (AI trả hay template tự sinh) còn API bịa, còn khối hở, thiếu await hay sai cú pháp đều bị chặn, không bao giờ tới tay người chơi. Chốt chặn này cắm chung cho cả EJS Studio nên mọi đường sinh EJS đều được soi bằng một thước.';
