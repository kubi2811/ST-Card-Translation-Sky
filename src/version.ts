// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.21';
export const APP_VERSION_NOTE = 'Trợ Lý AI — P1 roadmap: RAG MVP. Trợ lý giờ TỰ TÌM đoạn liên quan trong lorebook (FULL — trước chỉ có preview cắt ngắn) + file đính kèm (PHẦN i/N) + kho kiến thức MVU rồi đưa cho AI KÈM NHÃN NGUỒN "(nguồn: file › mục)" — buộc AI trích dẫn khi dùng ⇒ trả lời bám tài liệu, giảm bịa. Kỹ thuật: semanticChunker (chunk 400-1600 ký tự theo ranh giới câu/đoạn, khối code giữ nguyên, bảo toàn 100%), hash-embedding n-gram 512d offline (0 tải model, 0 tốn quota — đủ cho near-match kiểu Translation Memory; interface chốt sẵn để nâng lên API/WASM embedding ở P5), RagIndex hybrid: EXACT thuật ngữ glossary/TM (ưu tiên tuyệt đối) > keyword > vector cosine; lọc theo card đang mở. Index dựng lười lúc idle, không chặn UI. Toggle "Trí nhớ RAG" (mặc định bật) + tooltip. Đo live card Long Tộc: 245 entry → 2.057 chunk, query 40ms (KPI <50ms đạt). +9 test. | 1.99.20: nền móng trí nhớ IndexedDB.';
