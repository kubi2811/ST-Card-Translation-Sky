// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.9.0';
export const APP_VERSION_NOTE = 'Chiến lược A — dịch card Mythic (Auto Database): dịch mô tả + điều kiện kích hoạt của skill Agent trong khối JSON ở chú thích entry, rồi TỰ TÍNH LẠI mã kiểm tra (hash) để script không coi thẻ là đã bị sửa hỏng. Giữ nguyên tiêu đề những entry phải suy thời đại từ từ khoá. Chạy riêng hoặc song song với Chiến lược B/C.';
