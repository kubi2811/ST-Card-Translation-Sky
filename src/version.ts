// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.2.1';
export const APP_VERSION_NOTE = 'Fix 3 bug user báo 19/07. (1) TRỢ LÝ AI "Lỗi gọi API: Aborted" cả 3 lần thử: gốc rễ là timeout cứng 120s đếm TỔNG THỜI GIAN — câu trả lời dài đang stream ngon lành cứ chạm mốc 120s là bị chém giữa chừng, retry lần nào cũng chết đúng mốc đó, còn thông báo lỗi thì bị nuốt còn đúng 1 chữ "Aborted". Nay 120s là WATCHDOG IM LẶNG: đồng hồ reset mỗi khi nhận thêm chunk dữ liệu — stream khoẻ chạy bao lâu cũng được, chỉ lane im bặt (đúng ca màn sleep) mới bị cắt; lỗi abort giờ GIỮ NGUYÊN lý do thật ("Quá 120s không nhận thêm dữ liệu…") thay vì "Aborted" cụt lủn; và hedge 30s chỉ bắn bản dự phòng khi CHƯA nhận byte nào (đang stream thì không bắn kép — đỡ tốn quota + không tự giết bản đang chạy tốt). +3 test hedge. (2) KHÔNG TUÂN THỦ Custom Translation Prompt — dịch cả entry tiếng Anh dù prompt khai FROM 中文: shouldSkipTranslation trước chỉ né ngôn ngữ ĐÍCH, lơ ngôn ngữ NGUỒN user khai; entry thuần Anh vào tay AI là bị luật "không sót ký tự ngoại ngữ" ép dịch nốt. Nay nguồn khai CỤ THỂ (không phải auto) + field chắc chắn thuộc ngôn ngữ THỨ BA (Anh/Nhật/Hàn… ≠ nguồn ≠ đích) → SKIP ngay tại máy, không đưa cho AI — deterministic, không trông chờ AI nghe lời. Nguồn "auto" giữ nguyên hành vi cũ; text trộn ngôn ngữ vẫn dịch bình thường. +7 test. (3) BẢNG FIELD hiển thị thiếu dù kéo xuống đáy (DOM/data có đủ): 2 lỗi trong bảng ảo hoá — key theo path nên 2 field TRÙNG path (sau luồng So Sánh/gộp) bị React lặng lẽ vứt bớt row; và cache chiều cao đánh theo index nên danh sách đổi độ dài (lọc/gộp) là tổng chiều cao spacer sai → cuộn tới "đáy" scrollbar mà chưa phải đáy thật. Nay key kèm index + measure() lại khi độ dài đổi (cả 2 view bảng/thẻ). 463 test xanh. | 2.2.0: Tool tạo Template Preset (5 khối).';
