// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.4.0';
export const APP_VERSION_NOTE = '⚡ KHỞI ĐỘNG NHẸ (lazy-start): start.bat giờ CHỈ mở Dịch Card (1 cửa sổ duy nhất) thay vì bung 5 cửa sổ CMD. Các tool khác (Tạo Card / Tạo Preset / Mod Card / Web Crawler) chỉ khởi động khi bạn BẤM TAB tương ứng — server chạy ngầm không bung cửa sổ, lần đầu ~30s (cài thư viện), các lần sau vài giây. Icon trên thanh bên có chấm trạng thái: XANH = đang chạy, XÁM (icon mờ) = đang tắt, VÀNG nhấp nháy = đang khởi động; Trích Card là file tĩnh nên luôn sẵn sàng. Trong tab có nút "Dừng server" để tắt tool không dùng cho nhẹ máy (tắt xong bấm lại tab là có nút khởi động lại, không tự bật). Màn chờ hiện đồng hồ giây + log khởi động thu gọn; lỗi khởi động thì hiện đúng lý do + nút Thử lại. Server bạn tự mở tay (npm run dev) vẫn được Hub nhận diện và dùng lại luôn. Đóng Hub = tự dọn hết server con đã spawn. Muốn chạy sẵn cả 5 tool như trước? Dùng start-all.bat (mới thêm). API /api/tools/* có CSRF guard như các endpoint khác. | 2.3.1: Chế độ Đồng Nhân chống Hán-Việt hoá tên IP Nhật/Hàn + fix nameStyle rơi ở chế độ chuyên gia.';
