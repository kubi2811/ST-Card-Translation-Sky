// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.1.2';
export const APP_VERSION_NOTE = '2 fix theo bugNeedFix/31 + feedback: (1) DỊCH CODE HTML/JS LỖI (surgical) — phân tích 3 file gốc/tool/claude tìm ra 2 lỗi gốc: (A) ĐỤNG ĐỘ ĐỊNH DANH: 女性角色(Nữ) bị dịch TRÙNG "Nhân Vật Nam" như 男性角色(Nam) ⇒ code tra nhầm object (tường Nữ đọc data Nam). Nay detectSurgicalIdentifierCollisions bắt ≥2 khóa/định danh nguồn khác nhau ra cùng bản dịch → gọi AI dịch LẠI cho khác nhau + ép duy nhất. (B) THÊM "- " VÀO KEY JSON: "秦鱼"→"- Tần Ngư" (AI tưởng object là danh sách) ⇒ tra ảnh hỏng, mất ảnh nhân vật; nay strip sạch ký tự đầu dòng (-,*,•,+) khỏi bản dịch định danh. + tăng cường prompt surgical (rule 10 UNIQUE cặp đối lập Nam/Nữ, rule 11 NO LIST MARKER). (2) CODE VĂN RA NGOÀI KHUNG khi trợ lý mở fence GIỮA DÒNG ("...nhé! ✨ 🖼️ ```javascript\\n<code>"): bản vá trước ép fence ở đầu dòng nên fence mở giữa dòng không nhận → code đổ thành text trần. Nay nhận fence mở đầu dòng HOẶC sau khoảng trắng (theo sau là lang+xuống dòng) mà vẫn không nhầm ``` trong code (đứng sau /,^,"). +12 test. Verify live: 女/男 collision bắt đúng, "- " strip, code vào 1 khung không lọt text. | 2.1.1: giao diện Việt trọn vẹn.';
