// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.1.7';
export const APP_VERSION_NOTE = '3 fix theo bugNeedFix/37-38 + khoá từ điển: (37) IMPORT CARD LỚN ĐƠ MÁY — đo bằng chính card user (PNG 4.3MB, JSON 1.37MB, 199 entry, tavern_helper 692KB): setCard kích 1 render tick trong đó 4 quét nặng chạy ĐỒNG BỘ (extract field LẶP việc worker + acorn parse toàn bộ script + quét MVU/Zod 692KB + chấm điểm preset ×2) → nghẽn 1 core nhiều giây (đúng hiện tượng RAM dư, CPU <40%). Nay: hook useIdleMemo hoãn từng quét ra idle tick riêng SAU khi UI vẽ; ExportPanel chuyển selector hẹp + throttle fields 300ms + NGƯNG quét health khi đang dịch (trước đây acorn 692KB chạy lại theo MỖI updateField); recommendPreset cache WeakMap (hết chạy ×2). Đo thật: tổng block 704ms, block đơn max 269ms — hết "trang không phản hồi". (38) KIỂM SÂU BÁO "MACRO DAMAGED" GIẢ — bộ kiểm đòi {{...}} nguyên văn; nay phân loại: PLACEHOLDER ({{chữ CJK}} không "::") được DỊCH ruột, chỉ lỗi khi MẤT (đếm); macro LỆNH mọi loại (kể cả format_message_variable) cho phép đổi arg theo từ điển MVU map TỪNG ĐOẠN path chấm + fallback đủ-số-macro-cùng-type; damage thật ({{char}} mất, vỡ ngoặc) vẫn bắt. +9 test. (KHOÁ DICT) nút 🔒 "Khoá từ điển" ở Chiến lược B: cấm pipeline dịch tự thêm/sửa/chuẩn hoá/dọn mvuDictionary (12 đường ghi tự động đều qua writeMvuDictAuto tôn trọng khoá; khoá cũng GIỮ dict khi import card mới); sửa tay trong panel vẫn OK. i18n vi/en/zh. 446 test xanh. | 2.1.6: nút Dừng + timeout cứng Trợ Lý AI.';
