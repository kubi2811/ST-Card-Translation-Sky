// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.3.0';
export const APP_VERSION_NOTE = 'Đợt nâng cấp 19/07 theo phản hồi user. AUTO CREATOR (Tạo Card): (1) KIỂM MVU ngay sau khi tạo — schema phải dựng được Zod code thật (đúng thứ SillyTavern chạy), đủ entry [initvar]/[mvu_update]/varList đã yêu cầu; thiếu là pipeline TỰ CHẠY LẠI bước với lý do ghi rõ trong log (chữa đúng ca "tạo được MVU nhưng chơi card báo 变量更新失败"). (2) TÍCH HỢP Regex ↔ MVU: đảo thứ tự pipeline — MVUZOD nay chạy TRƯỚC Regex, và schema biến được bơm TƯỜNG MINH vào prompt regex kèm luật "data-var PHẢI dùng đúng tên biến schema, cấm bịa" → dashboard với biến hết lệch nhau. (3) BƯỚC MỚI "Kiểm tra tổng thể" cuối pipeline: kiểm deterministic toàn card (tên/description/first_mes, schema dựng Zod OK, có [initvar], có update rules, data-var khớp schema, regex compile, chất lượng + cấu hình lorebook) + tuỳ chọn 1 lượt AI đọc báo cáo và chỉ đúng bước cần chạy lại — kết quả nằm ở log + preview. (4) BƯỚC MỚI "Game UI": tự sinh status bar + form điền thông tin thiết lập ban đầu TỪ SCHEMA bằng bộ dựng programmatic ($0 — không tốn lượt AI nào, kết quả ổn định), chọn được trọn bộ / chỉ status bar / chỉ form. (5) Ô MỚI "Yêu cầu / quy tắc cho AI" cạnh ô Ý tưởng — ràng buộc toàn cục áp cho MỌI bước (khác Ý tưởng: đây là luật, không phải nội dung). (6) Về "kế thừa giữa các tiến trình" bạn hỏi: ĐÃ CÓ SẴN — mỗi bước đọc lại toàn bộ card mà các bước trước vừa ghi vào; lưu ý nó chỉ trọn vẹn khi TẮT "Preview trước khi apply" (bật preview thì bước sau chưa thấy kết quả bước trước cho tới khi bạn bấm Apply). Kèm fix nhỏ: bước Lorebook trước đây là bước DUY NHẤT lơ promptOverride của user — đã vá. TRỢ LÝ AI (Dịch Card): mục mới "📜 Prompt Chỉ Thị" trong tab cài đặt của panel — các quy tắc bạn gõ vào (vd "Cấm dịch tiếng Anh", "Phải chất vấn, hỏi rõ chi tiết trước khi làm") được chèn vào CUỐI system prompt với khung TUÂN THỦ TUYỆT ĐỐI, ưu tiên cao hơn mọi hướng dẫn khác; tự lưu khi gõ, có badge "đang áp dụng" + nút xoá. i18n đủ vi/en/zh cả 2 app. | 2.2.3: fix treo vô hạn khi bấm Dịch (backtracking extractZodDescriptions).';
