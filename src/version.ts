// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.16.7';
export const APP_VERSION_NOTE = 'Tạo Card — fix bug 99 + làm bug 98. Bug 99 (MVUZOD quét AI treo ở bước Tổng hợp, nút Dừng quét bấm không ăn): bước tổng hợp trước đây nhồi JSON của mọi batch vào một prompt rồi nhờ AI gộp ở chế độ bắt-buộc-JSON — output luôn chạm trần token nên vòng “viết tiếp phần còn thiếu” gửi lại cả lịch sử đang phình, tới 4 lượt, mỗi lượt một chậm; tệ hơn là ở chế độ đó mỗi lượt model trả một object mới nên nối lại chẳng bao giờ ra JSON hợp lệ. Nay gộp BẰNG CODE (hợp nhất field theo path, gộp enum, giữ ràng buộc rộng nhất): 20 batch gộp xong trong 1 mili giây, không tốn call nào. Nút Dừng nay cắt đứt luôn request đang bay chứ không chỉ bật một lá cờ, và nếu đã quét được vài batch thì vẫn giữ kết quả đó. Bug 98 (thiếu bước kiểm tra chéo): thêm BƯỚC MÔ PHỎNG chạy thật vòng đời thẻ trước khi coi là xong — nạp [initvar] (giờ đọc được cả YAML, trước chỉ chịu JSON nên với thẻ do chính app sinh thì phép kiểm sâu nhất bị bỏ qua im lặng), đối chiếu hai chiều với schema, giả lập Opening Form ghi rồi đọc lại, chạy thật lệnh <UpdateVariable>, và soi mọi biến mà EJS/thanh trạng thái đọc xem có tồn tại không. Kèm nút “Kiểm & Mô phỏng lại” chạy trọn vòng kiểm tra → vá → mô phỏng, không tốn API, bấm được bất cứ lúc nào kể cả vừa mở lại app hay vừa sửa tay một biến/hàm ở đâu đó.';
