// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.2.3';
export const APP_VERSION_NOTE = 'BUG 39C — TREO khi bấm "Bắt đầu dịch" sau luồng gộp So Sánh (log dừng ở "Pha 0 — bảng tên riêng"). Truy bằng đo thật trên chính 3 card của bạn (bugNeedFix/40): liệt kê từng bước sync/await của startTranslation + đo từng hàm trong Node. Kết quả bisect: mọi regex scanner đều vô tội (<400ms), thủ phạm là extractZodDescriptions — regex cũ chứa (?:\\.\\w+\\([^)]*\\))* tức QUANTIFIER LỒNG không chặn ⇒ catastrophic backtracking: script MVU 525K đầy chuỗi z.number().min().max() KHÔNG có .describe làm regex chạy quá 10 PHÚT không xong 1 script (đo được: process Node treo cứng). extractPotentialMvuKeys gọi hàm này cho TỪNG script TavernHelper ngay sau khi Pha 0 trả lời — cả đoạn đó là MỘT KHỐI SYNC liền trên main thread nên log sau Pha 0 không kịp vẽ ⇒ đúng hiện tượng "Trang không phản hồi" đứng im ở dòng Pha 0. FIX: (1) viết lại extractZodDescriptions TUYẾN TÍNH — neo tại từng ".describe(" rồi đi lùi qua chuỗi method bằng máy quét tay có chặn, không còn backtracking; extractPotentialMvuKeys trên card 1.6 từ TREO VÔ HẠN xuống còn 365ms. (2) Vá luôn regex cùng lớp bệnh (leading \\w+) trong getMvuCardSummary/mvuDetector — 94ms trên card này nhưng là bom nổ chậm với card khác. (3) Thêm 2 điểm NHẢ main thread trong startTranslation (trước cụm dò MVU và trước cụm quét EJS) làm lưới bảo hiểm: hotspot mới nếu có sẽ chỉ làm chậm, không treo trắng nữa, và log luôn kịp hiện. Đã chạy đúng kịch bản của bạn trên dev: nạp 3 PNG → Gộp 418/187 → Đưa sang → BẤM DỊCH — pipeline chạy xuyên qua điểm treo cũ ("Chiến lược B… Thấy 173 biến"), long task lớn nhất 466ms, trang sống suốt. +7 test khoá tính đúng + guard hiệu năng (chuỗi zod không describe phải xong <300ms — regex cũ treo vô hạn). 470 test xanh. | 2.2.2: ảo hoá bảng So Sánh + normalize schema enumValues + lưu Tiêu chí hoàn thành.';
