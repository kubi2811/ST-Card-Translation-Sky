// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.1.9';
export const APP_VERSION_NOTE = 'Tạo Card — 4 việc. (1) FIX NẶNG: thẻ xuất ra PNG KHÔNG import được vào SillyTavern. Gốc rễ: khi ghi chunk metadata, byte thứ 3 của tên chunk bị đặt nhầm là 120 (chữ "x" thường) → chunk ra "tExt" thay vì "tEXt". SillyTavern đọc thẻ bằng png-chunks-extract rồi LỌC đúng tên "tEXt", nên không thấy chunk nào ⇒ báo "No PNG metadata" và bỏ qua thẻ — ảnh vẫn mở xem bình thường nên rất khó đoán. Đã đối chiếu trực tiếp với mã nguồn SillyTavern (src/character-card-parser.js + png-chunks-extract) và chạy thử: TRƯỚC fix parser trả về "tExt,tExt" → không đọc được; SAU fix trả về "tEXt,tEXt" → đọc ra đúng JSON cả tiếng Việt lẫn chữ Hán. Bộ đọc/dọn chunk nay chấp nhận thêm "tExt" cũ, nên thẻ lỡ xuất bằng bản lỗi chỉ cần xuất lại là sạch, không sót chunk hỏng. +4 test khoá lại (tên chunk, đủ 2 chunk ccv3+chara, CRC khớp, xuất lại 2 lần không nhân đôi). (2) AI Sinh Theo Batch: toàn bộ 17 thiết lập + nội dung 7 tab prompt nay LƯU LẠI (bgen.*), không còn mất khi đóng/mở lại. (3) Lorebook: bấm thẳng vào tên ở đầu trang để ĐỔI TÊN (Enter hoặc bấm ra ngoài là lưu, Esc huỷ, tên cũ được bôi đen sẵn); thẻ chưa có lorebook cũng tạo được tên. (4) Ô "Ý tưởng của bạn": thêm nút Mở rộng phóng to toàn màn hình + đếm ký tự, tiện soạn ý tưởng dài. | 2.1.8: bug 39 treo màn khi dịch qua So Sánh/Update card.';
