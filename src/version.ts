// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.16.6';
export const APP_VERSION_NOTE = 'Tạo Card — fix bug 97 (script 酒馆助手 sinh ra chưa dùng được). Ba lỗi: (1) script “Cấu trúc biến …” chỉ có `export const Schema = …` mà thiếu dòng import mvu_zod.js và lệnh `registerMvuSchema(Schema)` nên schema khai báo xong nằm im, MVU không bao giờ nhận — nay tự bọc đủ (bọc idempotent, thẻ cũ dán vào cũng cứu được); (2) công tắc “Nút” của script MVU bị ghi tắt sẵn nên 6 nút của bundle (Xử lý lại biến, Đọc lại biến khởi tạo, Chụp/Diễn lại tầng…) không hiện cho người chơi — nay bật đúng mặc định của TavernHelper; (3) khoá tên biến có dấu cách bị viết trần trong code Zod (Thế Giới: z.object) gây SyntaxError chết cả file — nay luôn bọc nháy. Ghi chú: KHÔNG đổi chuẩn tên biến sang dấu _ như đề xuất, vì chuẩn dấu cách đã chốt ở bug #8 và việc ép _ trước đây từng làm 272 chỗ trong thẻ lệch nhau; bọc nháy chữa đúng gốc lỗi mà vẫn giữ tên biến đồng nhất với thẻ đã dịch. Bộ kiểm tổng thể (final_check) nay soi luôn 6 lỗi này của script 酒馆助手.';
