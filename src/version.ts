// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.1.5';
export const APP_VERSION_NOTE = 'Fix HIỆU NĂNG theo bugNeedFix/36 + #5: (36) DỊCH CARD LỚN TREO "TRANG KHÔNG PHẢN HỒI" — khi dịch 600+ trường đa luồng, engine gọi updateField/addLog HÀNG NGHÌN lần; các panel tiến độ (TranslationPanel/ModModePanel/ActiveCallsPanel/LogPanel/LogFilterBar) lại `useStore()` KHÔNG selector nên subscribe TOÀN store → mỗi write re-render + filter/reduce cả mảng 605 → nghẽn main thread. Nay: (a) hook useThrottledStore gộp nhịp render ~7-8 lần/giây (engine vẫn đọc getState ĐỒNG BỘ — 0 đổi luồng dữ liệu); (b) các panel chuyển sang selector HẸP + throttle `fields`/`logs`; (c) bỏ destructure `logs` THỪA ở 2 panel (re-render mỗi log vô ích); (d) memo LogRow + useMemo lọc/gom log (khỏi reconcile 300 dòng mỗi addLog). Field list vốn ĐÃ virtualize nên không phải thủ phạm. (#5) IMPORT/ MỞ TAB MVU ĐƠ 10-50s — panel Chiến lược B render HẾT vài nghìn dòng dict thành DOM. Nay CAP 150 dòng/nhóm + gợi ý gõ ô tìm để lọc (dịch vẫn dùng đủ mọi key). i18n vi/en/zh. 437 test xanh. | 2.1.4: trạng thái bật/tắt regex + Ký ức xoá tất cả/giữ cuộn.';
