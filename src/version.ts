// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.73.0';
export const APP_VERSION_NOTE = 'MỚI: POPUP GỢI Ý CẤU HÌNH sau khi import card. Nạp card mới → app phân tích xong hiện popup: preset khuyên dùng (★) + lý do, 2 nút "✅ Dùng cấu hình gợi ý" (áp ngay 1 chạm — bật/tắt nhóm field, MVU, chế độ phẫu thuật… đúng như bấm nút preset) hoặc "Giữ cấu hình hiện tại" (đóng, không đổi gì). Card khôi phục tiến trình cũ thì KHÔNG hiện (không quấy rầy việc đang dở). Người mới: nhập key → nạp card → bấm 1 nút là dịch đúng chế độ. | 1.72: tự phân tích card → gợi ý preset (sao ★ + dòng giải thích). | 1.71: sửa bug #4 Dịch nhẹ vỡ MVU (tên biến có dấu cách).';
