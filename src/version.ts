// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.9.1';
export const APP_VERSION_NOTE = 'Chiến lược A (card Mythic) chạy được thật: nhóm field mythic đã được nối vào đường dịch, và khối JSON meta trong chú thích entry được TÁCH khỏi đường dịch — AI chỉ thấy tiêu đề, khối meta ghép lại nguyên vẹn kèm mã kiểm tra tính lại. Trước đó AI được đưa cả 449k ký tự JSON, đủ để viết hỏng hash và eras của thẻ.';
