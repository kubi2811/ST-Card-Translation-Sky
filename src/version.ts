// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.7.9';
export const APP_VERSION_NOTE = "Bug 78: nhap Opening Form khong cap nhat bien. Do tren the that (bugNeedFix/41): bon noi khai bao bien khong cap nao giao nhau — schema Zod dung Player>Name, entry [initvar] dung khoa PHANG 'Player/Name:', Opening Form dung NHAN tieng Viet ('Cap Tang Hien Tai'), Status Bar co 0 data-var. Sua: (1) Opening Form lay ten bien tu `path` thay vi `label`; (2) initvar phang do AI viet duoc dung lai thanh cay YAML that. Con lai: form van ghi bang /setvar (sai kho luu) va Status Bar chua sinh data-var — chua sua.";
