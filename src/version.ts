// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.16';
export const APP_VERSION_NOTE = 'Trợ Lý AI — FIX code/câu trả lời VĂNG RA NGOÀI KHUNG + nút chật/tràn. GỐC BUG: bộ tách markdown cũ đóng fence ``` ở BẤT KỲ dấu ``` nào, kể cả dấu nằm GIỮA DÒNG bên trong code — mà trợ lý hay viết đúng loại code đó (text.replace(/```$/, \'\')) → fence đóng SỚM ⇒ nửa sau của code rớt ra ngoài, render thành VĂN BẢN TRẦN (không khung, không wrap) ⇒ tràn khỏi bong bóng chat, đè sang cột bên phải. FIX: fence chỉ tính khi ở ĐẦU DÒNG (chuẩn markdown) + fence mở chưa đóng (đang stream/bị cắt) vẫn hiện trong khung code; tách ra utils/chatMarkdown.ts + 6 test. Chống tràn 3 lớp: min-width:0 cho bong bóng/khối chat (flex item mặc định không co được → 1 dòng code dài đủ đẩy phình cả khung), overflow-wrap:anywhere cho văn bản (URL/chuỗi dài luôn bẻ dòng), pre cuộn ngang BÊN TRONG khung. Nút: 3 thanh (header code, Quick Inject, ô nhập) cho xuống hàng thay vì bóp dí/tràn. Đo live: tràn ngang = 0px, 0 phần tử vượt mép phải. | 1.99.15: Trợ Lý AI hedge + full pool.';
