// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.84.0';
export const APP_VERSION_NOTE = 'MỚI CHO DỊCH GIẢ: lỗi script trong preview 🧪 giờ TỰ TRA RA NGUỒN — báo rõ lỗi nằm trong regex/lời mở đầu nào của card kèm nút "↪ tới field" nhảy thẳng vào Field Editor để sửa bản dịch. Thêm "⇄ So 2 bản": Gốc và Đã dịch chạy CẠNH NHAU — nhìn ngay khung nào mất data/vỡ giao diện sau dịch; lỗi hiện riêng từng bên ([Gốc]/[Đã dịch]) → cả 2 bên cùng lỗi = lỗi có sẵn của card, chỉ bên Đã dịch lỗi = lỗi do dịch. | 1.83: 🧪 chạy script + data test từ [initvar]. | 1.82: fix popup click-through + tự nạp bộ tu tiên.';
