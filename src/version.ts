// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.2.2';
export const APP_VERSION_NOTE = 'Đợt fix 19/07 (phần 2). (1) BUG 39B — treo máy NGAY khi bấm "Đưa sang Dịch Card" (gộp 3 card, chưa vào giao diện dịch): đã ĐO lại toàn bộ pipeline dữ liệu bằng đúng 3 card thật của bạn — mọi hàm thuần (trích PNG, planMerge, extract 605 field, quét sức khoẻ, quét EJS) cộng lại chỉ ~330ms, nghĩa là thủ phạm KHÔNG phải logic mà là TẦNG GIAO DIỆN: bảng So Sánh render THẲNG 605 hàng × 3 ô nhập (~1.815 textarea thật trong DOM), cú bấm nút vừa dựng toàn bộ UI dịch phía sau vừa re-render cái bảng khổng lồ đó trong CÙNG một nhịp → main thread nghẽn chục giây → "Trang không phản hồi". Fix kép: (a) bảng So Sánh chuyển sang ẢO HOÁ (chỉ render phần đang nhìn thấy — cùng kỹ thuật bảng chính), (b) nút "Đưa sang" giờ ĐÓNG overlay TRƯỚC rồi mới nạp card/field ở nhịp sau. Đã tái hiện đúng kịch bản của bạn trên dev server (3 PNG → Gộp 418/187 → bấm nút): long task lớn nhất chỉ 80ms, giao diện dịch nhận đủ 605 field ngay. (2) Tạo Card — Auto Creator crash "Cannot read properties of undefined (reading enumValues)": schema AI sinh ra hay THIẾU key constraints (type khai bắt buộc nhưng JSON cast thô không kiểm) → consumer đầu tiên đọc là sập. Fix gốc: normalize schema tại MỌI biên dữ liệu vào (pipeline Auto Creator, setMvuzodSchema của store, parse SchemaWizard) — constraints luôn là object, enumValues dạng chuỗi tự tách thành mảng, children đệ quy sạch; kèm guard ?. phòng thủ ở 8 file consumer cho schema CŨ đã lưu trong máy; prompt bước mvuzod ghi rõ constraints là BẮT BUỘC. +7 test. (3) Tạo Card — Tiêu chí hoàn thành nay LƯU LẠI (bgen.criteria — cái duy nhất còn sót đợt trước), thêm nút "💾 Lưu cài đặt" (xác nhận tường minh + toast) và nút "🪄 Áp dụng vào Auto Creator" bơm cấu hình tab hiện tại (tổng entry, sàn entry lấy từ tiêu chí, batch, web search, prompt) thẳng vào bước Lorebook của Auto Creator — đã kiểm trên dev: giá trị sang đúng từng ô. | 2.2.1: idle-watchdog "Aborted" + skip ngôn ngữ thứ ba + key bảng ảo hoá.';
