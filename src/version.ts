// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.1.3';
export const APP_VERSION_NOTE = '3 fix theo bugNeedFix/32-34: (34) XEM TRƯỚC BẢN DỊCH TRẮNG MÀN — script trong iframe vỡ SyntaxError vì cv.已测能量 (dot-notation) KHÔNG được đổi sang bracket cv[\'已测能量\'] do bộ đoán "đang trong chuỗi" đếm nháy \' và " ĐỘC LẬP: một " nằm trong chuỗi nháy đơn \'<span class="tag \' làm _dq lẻ → tưởng đang mở chuỗi kép → tắt đổi bracket → dịch ra cv.Đã Đo Năng Lượng (có dấu cách = JS lỗi). Nay isInsideStringAtEnd quét trạng thái nháy ĐÚNG (nháy trong chuỗi khác không tính). (32) TRỢ LÝ AI CHỈ ĐỌC BẢN GỐC — ngữ cảnh card chỉ nạp `card` (tiếng Trung), bản dịch nằm ở store `fields` chưa ghi ngược ⇒ AI sửa lỗi nhỏ mù bản dịch. Nay đính KÈM bản dịch cạnh bản gốc cho mô tả + regex replaceString + lorebook content/comment (map path→translated). (33) DỊCH TỰ BỊA CODE (safeString) — KHÔNG phải AI bịa mà chính tool RA LỆNH: masterPrompt C3.4 + promptBuilder rule 25 bắt inject hàm safeString vào mọi Zod schema. Nay GỠ 2 lệnh đó, đổi thành CẤM thêm code (chỉ dịch chữ, giữ 100% cấu trúc) + LƯỚI TẦNG 2 verifyCodeStructureParity/detectInventedDeclarations: bắt khai báo lạ + ngoặc lệch → retry, hết retry giữ nguyên gốc (single + batch). +8 test (tổng 437 xanh). | 2.1.2: fix đụng độ định danh + "- " key + fence giữa dòng.';
