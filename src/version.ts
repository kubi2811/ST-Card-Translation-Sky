// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.9';
export const APP_VERSION_NOTE = 'Dịch Card — FIX TRIỆT ĐỂ bug biến nối "_" tràn vào lorebook (bugNeedFix/8: 272 chỗ Lưu_Tam_Bảo/Giới_Hạn_Từ_Bi, initvar trộn 8 key space + 8 key "_"). GỐC KÉP: (1) prompt dịch biến MVU cũ ÉP underscore (Độ_Hảo_Cảm) ĐÁNH NHAU với chuẩn space ở promptBuilder/canonicalize → mỗi nơi 1 kiểu; (2) quy tắc token cũ đòi "mọi mảnh non-ASCII" nên từ Việt thuần-ASCII (Tam/Bi/User/Tin) làm cả từ lọt lưới. NAY CHUẨN DUY NHẤT = DẤU CÁCH: 1) Toàn bộ prompt (mvuSync/apiClient/masterPrompt) cấm AI nối "_", theo đúng từ điển. 2) unifyVarWordSeparators: từ Latin-có-dấu nối _/- → space (giữ marker _/$ đầu, giữ identifier stat_data/场景_sfw/evt_01), bỏ "_" rác cuối tên (Tô Yến Hề_). 3) enforceVariableCasing MÙ separator: Giới_Hạn_Từ_Bi khớp dict "Giới Hạn Từ Bi" và bị ép về đúng dạng ở mọi ngữ cảnh (getvar, _.get, mảng path, macro, YAML key) + key JS không nháy được BỌC NHÁY tự động (\'Giới Hạn Từ Bi\': hợp lệ cả JS lẫn YAML — không tái phát bug #4 SyntaxError; guard cú pháp JS v1.99.7 làm lưới đỡ). 4) SWEEP DICT-LESS unifyVietnameseUnderscoresInText: quét toàn văn từ Việt-có-dấu chứa "_" → space — chạy tự động sau mỗi field code/lorebook khi dịch VÀ trong nút "Đồng nhất tên biến MVU" → CARD ĐÃ NHIỄM import vào bấm 1 nút là sạch (không cần dict, không cần AI). 20 test mới bằng chuỗi thật từ card user. | 1.99.8: UI Strategy B.';
