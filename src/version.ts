// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.24';
export const APP_VERSION_NOTE = 'Trợ Lý AI — P4 roadmap (phase cuối): SUB-AGENT + SANDBOX THẬT. (1) Orchestrator route intent mỗi lượt chat → 4 sub-agent (Translator/CodeFixer/LoreArchitect/General) — mỗi agent có persona riêng + WHITELIST action riêng; mơ hồ thì về General đủ quyền như cũ (zero regression). (2) LỚP BẢO VỆ ACTION: action ngoài whitelist hoặc params sai schema zod bị CHẶN trước khi thực thi (cả đường auto-execute lẫn confirm) + báo rõ trong chat "🛡️ N action bị chặn" — AI trả lệnh lệch chuẩn không còn phá được card. (3) RUN_SCRIPT chuyển từ iframe allow-scripts (vẫn GỌI MẠNG được — lỗ hổng) sang QUICKJS-WASM: interpreter kín tuyệt đối (fetch/window/document/localStorage/XHR/indexedDB đều không tồn tại), interrupt CPU 5s chặn while(1), RAM 64MB, dữ liệu card đưa vào là BẢN SAO (global input — sửa gì cũng không lan ra app). +10 test gồm bộ ESCAPE TEST. Verify live browser: escape = undefined ×4, while(true) bị giết, input copy nguyên vẹn. Roadmap P0→P4 hoàn tất — P5 (HNSW, embedding API/WASM, tree-sitter) chờ đạt điều kiện kích hoạt. | 1.99.23: code intelligence.';
