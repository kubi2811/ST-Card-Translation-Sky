// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.74.0';
export const APP_VERSION_NOTE = 'MỚI: PHA 0 — BẢNG TÊN RIÊNG TỰ ĐỘNG. Bấm Start, app quét cục bộ (0 token) các tên nhân vật/địa danh/thuật ngữ lặp lại trong card + keyword lorebook, dịch cả bảng bằng 1 lượt gọi AI (Hán-Việt: 叶凡→Diệp Phàm) rồi nạp vào từ điển — MỌI luồng dịch song song dùng chung ⇒ hết cảnh cùng 1 nhân vật mỗi entry dịch 1 kiểu. Từ điển to giờ chỉ nhét vào prompt những tên THẬT SỰ có trong đoạn đang dịch (đỡ phình token). Mục user tự nhập luôn thắng; tắt được trong mục Từ điển. | 1.73: popup gợi ý cấu hình sau import (2 nút Dùng/Giữ). | 1.72: tự phân tích card → gợi ý preset ★.';
