// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.90.0';
export const APP_VERSION_NOTE = 'Mod Card (bug #14, Ngổng): CHUYỂN SANG GIAO DIỆN TỐI đồng bộ với các tool khác — trước đây panel "Cài Đặt Cấu Hình LLM & Proxy" + các khung để nền TRẮNG chói trên nền tối, nay toàn bộ nền/chữ/viền/ô nhập đã tối, các hộp màu (xanh/vàng/đỏ) đổi thành tông tối dễ đọc. Phần Cài Đặt cũng thêm dòng nhắc "Tự động lưu" (đã xác minh: gõ URL proxy/API key rồi F5 vẫn còn, không cần nút Lưu — lưu ngay vào trình duyệt). | 1.89: Dịch Nhẹ dịch entry [mvu update]. | 1.88: glossary tự dọn + tavern helper export + card thường không tự chạy MVU/EJS.';
