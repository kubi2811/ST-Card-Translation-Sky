// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.83.0';
export const APP_VERSION_NOTE = 'MỚI: 🧪 CHẠY SCRIPT + DATA TEST trong "Xem như SillyTavern" — giả lập môi trường TavernHelper/MVU theo đúng extension thật (JS-Slash-Runner): phủ kín 178 API, bơm jQuery/lodash/Vue/YAML/zod như ST, DATA TEST lấy từ entry [initvar] của card → thanh trạng thái/game UI script tự đổ số y như đang chơi, khỏi cần import vào ST. Script chạy trong iframe cách ly tuyệt đối (không đụng được app); lỗi script hiện ngay trong modal — công cụ bắt "biến bị dịch vỡ". Verify bằng card Đạo Uyên v5.2 (ERA 282KB script): UI đầy đủ Sinh Mệnh/Linh Lực/tab Tổng quan…, 0 lỗi. Fix kèm: preview hết kẹt scroll với card dùng position:fixed/100vh. | 1.82: fix popup click-through + tự nạp bộ tu tiên.';
