// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.89.0';
export const APP_VERSION_NOTE = 'Dịch Card (bug #13, PhatSiz): ở chế độ "Dịch Nhẹ" (giữ nguyên content tiếng gốc) NAY tự TICK & dịch các entry [mvu update] — entry chứa marker [mvu update]/[mvu_update] trong nội dung, hoặc comment/tên là quy tắc cập nhật biến (controller). Đây là quy tắc cập nhật biến MVU mà người dịch cần ra tiếng đích, không để lẫn tiếng Trung như content thường. Áp cả ở nút preset (hiện tick ngay) lẫn lúc bấm Dịch. | 1.88: fix #10 glossary tự dọn + #11 tavern helper export + #12 card thường không tự chạy MVU/EJS.';
