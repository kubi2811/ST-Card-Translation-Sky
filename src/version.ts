// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.7.7';
export const APP_VERSION_NOTE = "Sua bao dong gia vua gay ra o v2.7.6: Kiem tra tong the bao nham script '[AI] Loai bo khoi UpdateVariable' la 'script render rong'. Script do co replaceString rong la DUNG — viec cua no la XOA khoi, khong phai render. Nay chi soi script bam vao MO NEO giao dien (<OpeningFormImpl/>, <StatusPlaceHolderImpl/>) moi tinh la render.";
