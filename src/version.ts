// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.1.0';
export const APP_VERSION_NOTE = '🧩 SCRIPT NẶNG (CHIA PHẦN) — Regex Manager › tab "Dịch link ngoài": bundle Vue/webpack 3.000.000+ ký tự trước đây dán nguyên khối làm AI cắt cụt ~100k đầu / treo UI. Nay khi script vượt NGƯỠNG (mặc định 100k, CHỈNH ĐƯỢC) tự cảnh báo + đề xuất chế độ Script Nặng. CHIA THÔNG MINH: scanner đọc từng ký tự, theo dõi trạng thái chuỗi/comment/độ sâu ngoặc + template ${} lồng + regex literal → chỉ cắt tại RANH GIỚI SẠCH (hết dòng, hoặc sau ; } ở độ sâu 0 với bundle minify) → KHÔNG bao giờ đứt giữa chuỗi sourcesContent/giữa hàm/giữa rule CSS. Xem trước danh sách Phần i/N + số ký tự trước khi dịch. Dịch TUẦN TỰ, tiến trình "Đang dịch phần 3/N"; DỪNG/TIẾP đúng phần dang dở (resume qua reload, không dịch lại từ đầu — fix lỗi #9); nút "Dịch lại phần này" cho riêng phần lỗi. GHÉP 1:1 (bất biến: ghép == gốc 100% từng ký tự, test khoá) + GLOSSARY CHUNG xuyên các phần cho nhất quán; dịch surgical giữ nguyên biến/key/class/thuộc tính. +6 test. Verify live: bundle 285k → 4 phần cắt sạch, ghép == gốc 100%. | 2.0.0: big update Trợ Lý AI.';
