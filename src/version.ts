// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.6.0';
export const APP_VERSION_NOTE = '🈶 TOOL MỚI "DỊCH PRESET" — tab mới trên thanh bên, dịch preset SillyTavern (JSON, 250+ prompts) Trung → Việt, dùng chung pool provider/API key của Dịch Card. Quy trình: nạp preset → xem inventory (bao nhiêu tên/nội dung/regex/script nhúng cần dịch) → Từ điển hợp nhất Pha 0 (1-2 lượt AI đề xuất: tên TAG tự chế 正文→Chính văn, tên BIẾN {{setvar}} 美型化→beautify chỉ ASCII, tên riêng — sửa tay thoải mái) → dịch song song theo lô → CODE thay tên tag/biến TOÀN CỤC NGUYÊN TỬ (AI không bao giờ tự đổi tên nên không thể lệch vế setvar/getvar) → vá regex → dịch script TavernHelper nhúng (đi nguyên pipeline Dịch Script với CÙNG từ điển — có cái là app JS 384KB) → kiểm tra: cấu trúc nguyên vẹn (identifier/thứ tự/field đóng băng/prompt_order), macro khớp tuyệt đối từng biến, regex compile hết. Regex chứa tiếng Trung NGOÀI từ điển (blocklist văn phong) được GIỮ NGUYÊN + liệt kê "cần chỉnh tay" — tự chế lại ngữ nghĩa regex là không an toàn. Tiến độ tự lưu vào cache máy (F5 dịch tiếp miễn phí). Đã kiểm với preset thật 三人逆行 v11.0 (253 prompts, 36 regex, script nhúng 384KB): inventory + từ điển + validate chạy đúng. i18n đủ vi/en/zh, 21 test mới (530 tổng). | 2.5.0: tool Dịch Script; 2.4.0: khởi động nhẹ lazy-start.';
