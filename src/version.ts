// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.7';
export const APP_VERSION_NOTE = 'Dịch Card: FIX TRIỆT ĐỂ script TavernHelper vỡ (bug 71K cụt đuôi giữa regex, mất 383 dòng mà vẫn "done"). 1) Guard CHUNK code: cụt output (<55%)/lệch cân bằng ngoặc/backtick lẻ → dịch lại đúng chunk, vẫn lỗi → giữ chunk gốc. 2) Guard FIELD: script gốc parse sạch (acorn, cả ES module import CDN lẫn script) mà bản dịch vỡ → retry → giữ nguyên gốc, chỉ rõ SỐ DÒNG lỗi — card KHÔNG BAO GIỜ xuất script liệt. 3) Code-heavy chunk 9k (chống chạm trần token output — gốc rễ vụ cụt). 4) Sức khoẻ thẻ/Kiểm tra tổng: soi JS TRẦN (trước chỉ soi <script> trong HTML) + card IMPORT có script GỐC vỡ sẵn được báo NGAY kèm dòng lỗi (chưa cần dịch). 5) Nút 🔧 Sửa nhanh gộp luôn script vỡ (revert về gốc). 6) Script chỉ import CDN (template cộng đồng) → tự bỏ qua không dịch (nội dung thật trên jsDelivr). 7) Glossary: tick chọn nhiều mục xoá 1 lần + nút Xoá TẤT CẢ (có xác nhận). 12 test mới (fixture = 2 file script thật). | 1.99.6: casing mảng path. | 1.99.5: quy luật keyword EJS.';
