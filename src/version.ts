// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.10';
export const APP_VERSION_NOTE = 'Xem như SillyTavern: chế độ 🧪 Chạy script giờ NẠP CẢ SCRIPT TAVERNHELPER CỦA CARD (Norma, Thiên Ý, Giám sát… — trong ST thật do 酒馆助手 nạp toàn cục, không nằm trong tin nhắn nên preview cũ không hiện). Card Long Tộc v8.1 giờ demo y hệt ST: panel NORMA SYSTEM + quả cầu nổi + quả cầu 诺. Mỗi script bọc try/catch gắn TÊN vào lỗi; script import framework CDN (mvu, zod) bỏ qua có ghi chú (cần ST thật, phần UI đã giả lập đủ). Dòng ghi chú hiện rõ script nào đã nạp / bỏ qua. | 1.99.9: fix triệt để biến nối "_".';
