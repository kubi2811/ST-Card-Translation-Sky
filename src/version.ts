// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.20';
export const APP_VERSION_NOTE = 'Trợ Lý AI — P0 roadmap (docs/ROADMAP_TRO_LY_AI.md): NỀN MÓNG TRÍ NHỚ. Kho dài hạn IndexedDB (Dexie) thay localStorage 5MB đã tràn: bảng memories (schema MemoryRecord có SOURCE GROUNDING — mỗi ký ức gắn nguồn file/PHẦN/field/lượt chat để truy vết), vectors (embedding, ghi lười vì tính lại được), conflictLog. Ghi WRITE-THROUGH + khoá Web Locks 1-writer đa tab + BroadcastChannel invalidate + version check (bản cũ hơn KHÔNG đè — vào conflictLog, không mất dữ liệu im lặng). Cache RAM tầng 2: LRU CÓ PIN theo ngân sách ký tự (pin = glossary/card đang mở không bao giờ bị đuổi; evict chỉ gỡ RAM, disk giữ vĩnh viễn). Decay score xếp hạng ký ức (recency + tần suất + pin). Migrate localStorage 1 lần khi idle + storage.persist() chống trình duyệt tự dọn + export/import JSON sao lưu. 10 test mới (fake-indexeddb). Đây là móng cho P1 RAG + P2 Loop. | 1.99.19: fix lorebook mất khúc đầu.';
