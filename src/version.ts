// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.82.0';
export const APP_VERSION_NOTE = 'FIX: (1) Popup gợi ý cấu hình hết bị "tự bấm rồi biến mất" — double-click chọn file trong hộp thoại làm cú click thứ 2 xuyên trúng nút popup vừa hiện; giờ popup khoá tương tác 450ms đầu. (2) Bộ thuật ngữ Tu tiên/Võ hiệp TỰ NẠP khi bấm Start nếu card có ≥8 thuật ngữ khớp (user khỏi phải nhớ bấm nút; đã xoá bớt thì không nạp lại; mục tự nhập luôn thắng). | 1.81: fix quanh xuất/đọc PNG. | 1.80: 👁 Xem như SillyTavern.';
