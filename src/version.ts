// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.99.15';
export const APP_VERSION_NOTE = 'Trợ Lý AI NHANH HƠN (feedback user "lâu quá, có xoay key/hedge như bên dịch không?"): 1) TRƯỚC ĐÂY Trợ Lý chỉ dùng PROVIDER #1 — không bơm provider phụ vào pool (chỉ Dịch Card làm) nên provider rảnh ngồi không; NAY bơm đủ pool → tự xoay lane (key/provider round-robin, né lane 429 đang nghỉ 15s). 2) THÊM HEDGE (trước chỉ Dịch Card có, chôn trong translateText): lượt gọi quá 30s chưa trả lời → tự bắn thêm 1 bản trên KEY/PROVIDER KHÁC, lấy bản xong trước, HUỶ bản thua (không tốn quota); lane treo không còn làm chờ vô hạn. Tách lõi ra utils/hedge.ts dùng chung + 5 test. 3) FIX GỬI TRÙNG: Enter 2 lần thật nhanh lọt guard isGenerating (state React cập nhật bất đồng bộ) → gửi 2 tin y hệt (đúng ảnh user); nay chặn bằng ref. 4) Hiện ĐỒNG HỒ CHỜ (giây) + báo rõ khi đang viết tiếp phần bị cắt / đã bắn bản dự phòng — hết cảnh spinner câm tưởng treo. | 1.99.14: preview Regex tự giãn.';
