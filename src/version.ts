// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '2.37.0';
export const APP_VERSION_NOTE = 'Bug 224 (Tạo Card · MVUZOD Studio). Bốn lỗi có bằng chứng, và ba trong số đó làm cả tab trông như hỏng. (1) 38 CẢNH BÁO TRÙNG TÊN PHẦN LỚN LÀ BÁO OAN: bộ Kiểm schema gom trùng theo NHÃN trên TOÀN CÂY, nên thẻ có 6 nhân vật mỗi người một biến "tình trạng" là ăn ngay cảnh báo "xuất hiện 6 lần — dễ ghi đè nhau", dù MVU địa chỉ hoá biến bằng ĐƯỜNG DẪN ĐẦY ĐỦ nên hai biến khác cha không bao giờ đụng nhau. Nay đếm theo CHA: cùng cha + cùng tên mới là trùng, và đó là LỖI thật (cùng một khoá object, cái sau xoá cái trước) chứ không phải cảnh báo; câu báo nêu đúng tên cha và giữ nguyên chữ hoa user đặt. (2) NÚT COPY BÁO THÀNH CÔNG GIẢ: Tạo Card chạy trong iframe của Hub mà iframe không được cấp clipboard-write, nên navigator.clipboard.writeText trả Promise bị từ chối — mọi chỗ gọi đều không await/không catch nên clipboard trống trơn mà nút vẫn hiện "Đã copy!". Nay một helper dùng chung (Clipboard API → lùi về execCommand chạy được trong iframe), hỏng cả hai đường thì toast nói thật và chỉ cách copy tay; áp cho cả 11 nút copy của MVUZOD lẫn EJS. (3) TAB GAME UI BỊ CẮT MẤT PHẦN DƯỚI: studio ép chiều cao theo viewport rồi nằm trong pane overflow-hidden, nên trên màn hẹp hai cột xếp dọc và ô nhập chat + nút "Tạo nhanh"/"Apply vào thẻ" nằm dưới đường cắt, không có thanh trượt để tới. Nay pane cuộn được, chiều cao ép chỉ áp từ breakpoint lg. Trợ lý AI của tab này vốn đã có đủ (nhận schema + initvar, tự chứng minh regex match sample, tự sửa 3 vòng) — nó chỉ nằm ngoài tầm mắt. (4) TAB PATCH "VÔ DỤNG" chỉ vì cái mẫu: chuỗi mẫu ghi cứng đường dẫn của một schema khác nên bấm Test trên thẻ nào cũng ra "path không tồn tại". Nay mẫu dựng từ chính schema đang mở, mỗi kiểu một thao tác phù hợp (number → delta, array → insert đuôi "-"), và ô nhập theo mẫu mới khi schema nạp sau. THÊM MỚI theo yêu cầu: nút "Nhờ AI sửa N vấn đề" ngay trong bảng Kiểm schema — gom mọi lỗi/cảnh báo thành một chỉ thị gọn (gộp theo loại, kèm cách sửa riêng từng loại và ràng buộc cấm xoá biến/đổi tên bừa/đổi kiểu) rồi đẩy sang ô AI chỉnh schema, vẫn qua bước xem trước từng action nên AI không tự tay đổi schema. 28 test mới, 1438 test Tạo Card xanh, tsc + eslint + build sạch. Verify trên trình duyệt thật: 6 nhân vật cùng tên biến ra 0 cảnh báo, mẫu patch sinh đúng đường dẫn thẻ, chỉ thị AI gộp đúng theo loại. Ghi chú bản cũ — Bug 219 + 221 + 222.';
