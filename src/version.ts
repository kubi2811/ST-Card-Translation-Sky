// ─── App version ───
// BUMP `APP_VERSION` on every fix so builds are distinguishable in the UI (shown in the
// sidebar header). Use the patch number for small fixes; keep `APP_VERSION_NOTE` to a one-line
// summary of the most recent change (shown on hover).
export const APP_VERSION = '1.80.0';
export const APP_VERSION_NOTE = 'MỚI: 👁 XEM NHƯ SILLYTAVERN — nút trong Card Preview mở modal render TĨNH lời mở đầu/greeting đúng kiểu ST: thay macro {{user}}/{{char}} + áp các regex script HIỂN THỊ của card, hiện trong iframe sandbox (script không chạy — an toàn tuyệt đối). Toggle Gốc/Đã dịch so trước–sau, chọn từng greeting, hiện tên regex đã áp. Thấy ngay giao diện in-chat sau dịch có vỡ không, khỏi cần import vào ST. | 1.79: prompt caching (RAG xuống cuối + đo ⚡cache). | 1.78: bộ thuật ngữ Tu tiên.';
